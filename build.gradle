// Root build file for the firma-management monorepo.
// The Spring Boot backend lives in :backend.
// The Angular frontend lives in /frontend (managed via npm, not Gradle).

allprojects {
    repositories {
        mavenCentral()
    }
}
